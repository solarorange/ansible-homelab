---
# Romm Homepage Integration

- name: Add Romm to homepage
  template:
    src: homepage-service.yml.j2
    dest: "{{ docker_config_root }}/homepage/services/romm.yml"
    mode: '0644'
  notify: reload homepage
  tags: [romm, homepage]
