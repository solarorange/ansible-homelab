---
# Romm Security Configuration

- name: Apply Romm security hardening
  template:
    src: security.yml.j2
    dest: "{{ docker_config_root }}/security/romm.yml"
    mode: '0644'
  tags: [romm, security]
