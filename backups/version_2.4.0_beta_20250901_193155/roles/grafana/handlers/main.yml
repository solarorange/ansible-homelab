---
# Grafana Role Handlers

- name: restart grafana
  community.docker.docker_compose_v2:
    project_src: "{{ docker_dir }}/grafana"
    state: present
    restarted: true
  tags: [grafana, restart]

- name: reload grafana
  community.docker.docker_compose_v2:
    project_src: "{{ docker_dir }}/grafana"
    state: present
    build: false
  tags: [grafana, reload]
