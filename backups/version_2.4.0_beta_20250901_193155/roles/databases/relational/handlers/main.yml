---
# Relational DB Handlers

- name: restart mariadb
  community.docker.docker_container:
    name: "{{ mariadb_container_name }}"
    state: started
    restart: true

- name: restart postgresql
  community.docker.docker_container:
    name: "{{ postgresql_container_name }}"
    state: started
    restart: true
