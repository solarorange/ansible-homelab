---
# Main entry for search sub-role (Elasticsearch, Kibana)

- name: Deploy Elasticsearch
  ansible.builtin.include_tasks: elasticsearch.yml
  when: elasticsearch_enabled | default(true)
  tags: [databases, search, elasticsearch]

- name: Deploy Kibana
  ansible.builtin.include_tasks: kibana.yml
  when: kibana_enabled | default(true)
  tags: [databases, search, kibana]

# Add more search DBs here as needed
