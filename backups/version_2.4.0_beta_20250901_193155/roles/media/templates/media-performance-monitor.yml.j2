# Performance monitoring config for media stack
services:
{% for service in media_services %}
  - name: {{ service.name }}
    metrics_url: {{ service.metrics_url }}
    cpu_threshold: {{ service.cpu_threshold | default(0.8) }}
    memory_threshold: {{ service.memory_threshold | default(500) }}
{% endfor %} 