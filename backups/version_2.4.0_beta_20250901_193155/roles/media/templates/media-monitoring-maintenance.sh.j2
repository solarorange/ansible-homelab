#!/bin/bash
# Media stack monitoring maintenance script
# Example: clean up old logs, rotate metrics, etc.
find {{ config_dir }}/media/logs -type f -mtime +30 -delete
# Add more maintenance tasks as needed 