---
# Simple App Role Tasks

- name: Include deployment tasks
  include_tasks: deploy.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, deploy]

- name: Include validation tasks
  include_tasks: validate.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, validate]

- name: Include monitoring tasks
  include_tasks: monitoring.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, monitoring]

- name: Include backup tasks
  include_tasks: backup.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, backup]

- name: Include security tasks
  include_tasks: security.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, security]

- name: Include homepage integration tasks
  include_tasks: homepage.yml
  when: simpleapp_enabled | default(true)
  tags: [simpleapp, homepage]
