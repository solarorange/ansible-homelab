# Ansible
*.retry
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv
ansible.cfg

# Vault files
*_vault.yml
*_vault.yaml
vault.yml
vault.yaml

# Local vault password files (never commit)
.vault_password
scripts/.vault_pass*

# Ansible working dir lock/state
.ansible/

# Sensitive files
**/credentials/
**/secrets/
**/keys/
**/*.pem
**/*.key
**/*.crt
credentials_backup.enc
credentials_backup.*.txt

# Local environment backups (never commit)
.env.bak
.env.backup.*

# Backup artifacts
**/*.backup*
backups/add_service_*/
backups/versions/*.tar.gz
backups/versions/*.commit
backups/versions/*.status
.last_backup_path

# Local inventories / test inventories
inventory.ini
inventory.txt
test_inventory.*
main.yml.backup

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/ 