---
# Fing Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Fing validation tasks
  include_tasks: validate.yml
  when: fing_validation_enabled | default(true)

- name: Include Fing deployment tasks
  include_tasks: deploy.yml
  when: fing_deploy_enabled | default(true)

- name: Include Fing configuration tasks
  include_tasks: configure.yml
  when: fing_deploy_enabled | default(true)

- name: Include Fing security tasks
  include_tasks: security.yml
  when: fing_deploy_enabled | default(true)

- name: Include Fing monitoring tasks
  include_tasks: monitoring.yml
  when: fing_monitoring_enabled | default(true)

- name: Include Fing backup tasks
  include_tasks: backup.yml
  when: fing_backup_enabled | default(true)

- name: Include Fing automation tasks
  include_tasks: automation.yml
  when: fing_automation_enabled | default(true)

- name: Include Fing integration tasks
  include_tasks: integration.yml
  when: fing_discovery_enabled | default(true)

- name: Include Fing health check tasks
  include_tasks: health_check.yml
  when: fing_health_check_enabled | default(true)
