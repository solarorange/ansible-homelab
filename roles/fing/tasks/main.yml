---- name: Include Fing validation tasks
  include_tasks: validate.yml
  when: fing_validation_enabled | default(true)
- name: Include Fing deployment tasks
  include_tasks: deploy.yml
  when: fing_deploy_enabled | default(true)
- name: Include Fing security tasks
  include_tasks: security.yml
  when: fing_deploy_enabled | default(true)
- name: Include Fing monitoring tasks
  include_tasks: monitoring.yml
  when: fing_monitoring_enabled | default(true)
- name: Include Fing backup tasks
  include_tasks: backup.yml
  when: fing_backup_enabled | default(true)
