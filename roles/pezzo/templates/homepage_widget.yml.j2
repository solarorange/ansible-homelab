---
# Pezzo Homepage Widget Configuration
# Custom widget for Pezzo service monitoring

pezzo_status:
  type: custom
  url: https://{{ pezzo_subdomain }}.{{ domain }}/api/healthz
  headers:
    Authorization: "Bearer {{ pezzo_api_token | default('') }}"
  options:
    title: "Pezzo Status"
    description: "AI Prompt Management Platform"
    icon: pezzo.png
    color: "#3B82F6"
    refresh: 30 