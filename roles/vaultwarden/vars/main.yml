---
# Vaultwarden Role Variables
# Static or environment-specific variables for Vaultwarden

vaultwarden_env: production
vaultwarden_timezone: "{{ timezone | default('UTC') }}"
vaultwarden_domain: "{{ domain }}"
vaultwarden_username: "{{ username | default('homelab') }}" 