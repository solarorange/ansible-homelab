---
# Komga Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Komga validation tasks
  include_tasks: validate.yml
  when: komga_validation_enabled | default(true)

- name: Include Komga deployment tasks
  include_tasks: deploy.yml
  when: komga_deploy_enabled | default(true)

- name: Include Komga configuration tasks
  include_tasks: configure.yml
  when: komga_deploy_enabled | default(true)

- name: Include Komga security tasks
  include_tasks: security.yml
  when: komga_deploy_enabled | default(true)

- name: Include Komga monitoring tasks
  include_tasks: monitoring.yml
  when: komga_monitoring_enabled | default(true)

- name: Include Komga backup tasks
  include_tasks: backup.yml
  when: komga_backup_enabled | default(true)

- name: Include Komga automation tasks
  include_tasks: automation.yml
  when: komga_automation_enabled | default(true)

- name: Include Komga integration tasks
  include_tasks: integration.yml
  when: komga_discovery_enabled | default(true)

- name: Include Komga health check tasks
  include_tasks: health_check.yml
  when: komga_health_check_enabled | default(true)
