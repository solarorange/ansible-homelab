---
# Main entry for cache sub-role (Redis)

- name: Deploy Redis
  ansible.builtin.include_tasks: redis.yml
  when: redis_enabled | default(true)
  tags: [databases, cache, redis]

# Add more cache DBs here as needed 