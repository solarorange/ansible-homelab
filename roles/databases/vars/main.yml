---
# Sensitive or environment-specific variables for Databases Role
# Override these in group_vars/all/vault.yml (encrypted with ansible-vault)

vault_postgresql_admin_password: "{{ vault_databases_database_password }}"
vault_mariadb_root_password: "{{ vault_databases_database_password }}"
vault_redis_password: "{{ vault_databases_database_password }}"
vault_elasticsearch_elastic_password: "{{ vault_databases_database_password }}"
vault_kibana_encryption_key: ""
