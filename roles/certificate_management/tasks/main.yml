---
# Certificate_Management Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Certificate_Management validation tasks
  include_tasks: validate.yml
  when: certificate_management_validation_enabled | default(true)

- name: Include Certificate_Management deployment tasks
  include_tasks: deploy.yml
  when: certificate_management_deploy_enabled | default(true)

- name: Include Certificate_Management configuration tasks
  include_tasks: configure.yml
  when: certificate_management_deploy_enabled | default(true)

- name: Include Certificate_Management security tasks
  include_tasks: security.yml
  when: certificate_management_deploy_enabled | default(true)

- name: Include Certificate_Management monitoring tasks
  include_tasks: monitoring.yml
  when: certificate_management_monitoring_enabled | default(true)

- name: Include Certificate_Management backup tasks
  include_tasks: backup.yml
  when: certificate_management_backup_enabled | default(true)

- name: Include Certificate_Management automation tasks
  include_tasks: automation.yml
  when: certificate_management_automation_enabled | default(true)

- name: Include Certificate_Management integration tasks
  include_tasks: integration.yml
  when: certificate_management_discovery_enabled | default(true)

- name: Include Certificate_Management health check tasks
  include_tasks: health_check.yml
  when: certificate_management_health_check_enabled | default(true)
