---
- name: Register storage services with homepage
  ansible.builtin.template:
    src: homepage-services.yml.j2
    dest: "{{ config_dir }}/homepage-services-storage.yml"
    owner: "{{ username }}"
    group: "{{ username }}"
    mode: "0644"
    force: true
