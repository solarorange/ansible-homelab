---
- name: Configure alerting for storage stack
  ansible.builtin.template:
    src: alerting.yml.j2
    dest: "{{ config_dir }}/alerting-storage.yml"
    owner: "{{ username }}"
    group: "{{ username }}"
    mode: "0644"
    force: true
