---
# Transmission Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Transmission validation tasks
  include_tasks: validate.yml
  when: transmission_validation_enabled | default(true)

- name: Include Transmission deployment tasks
  include_tasks: deploy.yml
  when: transmission_deploy_enabled | default(true)

- name: Include Transmission configuration tasks
  include_tasks: configure.yml
  when: transmission_deploy_enabled | default(true)

- name: Include Transmission security tasks
  include_tasks: security.yml
  when: transmission_deploy_enabled | default(true)

- name: Include Transmission monitoring tasks
  include_tasks: monitoring.yml
  when: transmission_monitoring_enabled | default(true)

- name: Include Transmission backup tasks
  include_tasks: backup.yml
  when: transmission_backup_enabled | default(true)

- name: Include Transmission automation tasks
  include_tasks: automation.yml
  when: transmission_automation_enabled | default(true)

- name: Include Transmission integration tasks
  include_tasks: integration.yml
  when: transmission_discovery_enabled | default(true)

- name: Include Transmission health check tasks
  include_tasks: health_check.yml
  when: transmission_health_check_enabled | default(true)
