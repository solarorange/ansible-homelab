---
# Jellyfin Monitoring Configuration

- name: Configure Jellyfin monitoring
  template:
    src: monitoring.yml.j2
    dest: "{{ docker_config_root }}/prometheus/jellyfin.yml"
    mode: '0644'
  notify: reload prometheus
  tags: [jellyfin, monitoring]
