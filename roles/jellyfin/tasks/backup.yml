---
# Jellyfin Backup Configuration

- name: Configure Jellyfin backup
  template:
    src: backup.sh.j2
    dest: "{{ docker_root }}/backup/jellyfin_backup.sh"
    mode: '0755'
  tags: [jellyfin, backup]
