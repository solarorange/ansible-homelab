---
# Jellyfin Alerting Configuration

- name: Configure Jellyfin alerts
  template:
    src: alerts.yml.j2
    dest: "{{ docker_config_root }}/alertmanager/jellyfin.yml"
    mode: '0644'
  notify: reload alertmanager
  tags: [jellyfin, alerts]
