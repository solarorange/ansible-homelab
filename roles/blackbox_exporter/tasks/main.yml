---
# Blackbox_Exporter Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Blackbox_Exporter validation tasks
  include_tasks: validate.yml
  when: blackbox_exporter_validation_enabled | default(true)

- name: Include Blackbox_Exporter deployment tasks
  include_tasks: deploy.yml
  when: blackbox_exporter_deploy_enabled | default(true)

- name: Include Blackbox_Exporter configuration tasks
  include_tasks: configure.yml
  when: blackbox_exporter_deploy_enabled | default(true)

- name: Include Blackbox_Exporter security tasks
  include_tasks: security.yml
  when: blackbox_exporter_deploy_enabled | default(true)

- name: Include Blackbox_Exporter monitoring tasks
  include_tasks: monitoring.yml
  when: blackbox_exporter_monitoring_enabled | default(true)

- name: Include Blackbox_Exporter backup tasks
  include_tasks: backup.yml
  when: blackbox_exporter_backup_enabled | default(true)

- name: Include Blackbox_Exporter automation tasks
  include_tasks: automation.yml
  when: blackbox_exporter_automation_enabled | default(true)

- name: Include Blackbox_Exporter integration tasks
  include_tasks: integration.yml
  when: blackbox_exporter_discovery_enabled | default(true)

- name: Include Blackbox_Exporter health check tasks
  include_tasks: health_check.yml
  when: blackbox_exporter_health_check_enabled | default(true)
