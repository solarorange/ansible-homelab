---
# Romm Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Romm validation tasks
  include_tasks: validate.yml
  when: romm_validation_enabled | default(true)

- name: Include Romm deployment tasks
  include_tasks: deploy.yml
  when: romm_deploy_enabled | default(true)

- name: Include Romm configuration tasks
  include_tasks: configure.yml
  when: romm_deploy_enabled | default(true)

- name: Include Romm security tasks
  include_tasks: security.yml
  when: romm_deploy_enabled | default(true)

- name: Include Romm monitoring tasks
  include_tasks: monitoring.yml
  when: romm_monitoring_enabled | default(true)

- name: Include Romm backup tasks
  include_tasks: backup.yml
  when: romm_backup_enabled | default(true)

- name: Include Romm automation tasks
  include_tasks: automation.yml
  when: romm_automation_enabled | default(true)

- name: Include Romm integration tasks
  include_tasks: integration.yml
  when: romm_discovery_enabled | default(true)

- name: Include Romm health check tasks
  include_tasks: health_check.yml
  when: romm_health_check_enabled | default(true)
