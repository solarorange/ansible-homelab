---
# Romm Monitoring Configuration

- name: Configure Romm monitoring
  template:
    src: monitoring.yml.j2
    dest: "{{ docker_config_root }}/prometheus/romm.yml"
    mode: '0644'
  notify: reload prometheus
  tags: [romm, monitoring]
