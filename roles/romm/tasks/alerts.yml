---
# Romm Alerting Configuration

- name: Configure Romm alerts
  template:
    src: alerts.yml.j2
    dest: "{{ docker_config_root }}/alertmanager/romm.yml"
    mode: '0644'
  notify: reload alertmanager
  tags: [romm, alerts]
