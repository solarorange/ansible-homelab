#!/bin/bash
# Utilities Stack Health Check Script (Placeholder)

# Check Portainer
if ! docker ps -f name=portainer | grep -q portainer; then
  echo "Portainer is not running"
  exit 1
fi

# Check Tdarr
if ! docker ps -f name=tdarr | grep -q tdarr; then
  echo "Tdarr is not running"
  exit 1
fi

# Check Tautulli
if ! docker ps -f name=tautulli | grep -q tautulli; then
  echo "Tautulli is not running"
  exit 1
fi

echo "All utility services are healthy."
exit 0 