---
# Media Role Tasks
# Enhanced with comprehensive security, automation, and validation

- name: Include Media validation tasks
  include_tasks: validate.yml
  when: media_validation_enabled | default(true)

- name: Include Media deployment tasks
  include_tasks: deploy.yml
  when: media_deploy_enabled | default(true)

- name: Include Media configuration tasks
  include_tasks: configure.yml
  when: media_deploy_enabled | default(true)

- name: Include Media security tasks
  include_tasks: security.yml
  when: media_deploy_enabled | default(true)

- name: Include Media monitoring tasks
  include_tasks: monitoring.yml
  when: media_monitoring_enabled | default(true)

- name: Include Media backup tasks
  include_tasks: backup.yml
  when: media_backup_enabled | default(true)

- name: Include Media automation tasks
  include_tasks: automation.yml
  when: media_automation_enabled | default(true)

- name: Include Media integration tasks
  include_tasks: integration.yml
  when: media_discovery_enabled | default(true)

- name: Include Media health check tasks
  include_tasks: health_check.yml
  when: media_health_check_enabled | default(true)
