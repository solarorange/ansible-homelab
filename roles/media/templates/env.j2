# Media Stack Environment Configuration
# Generated by Ansible - Do not edit manually

# System Configuration
PUID={{ user_id }}
PGID={{ group_id }}
TZ={{ timezone }}

# Network Configuration
MEDIA_NETWORK={{ media_network_name }}
HOMELAB_NETWORK={{ media_traefik_network | default('homelab') }}

# Domain Configuration
DOMAIN={{ domain }}

# Authentication
MEDIA_AUTH_METHOD={{ media_auth_method | default('authentik') }}
MEDIA_ADMIN_EMAIL={{ media_admin_email | default('admin@' + domain) }}

# Database Configuration
MEDIA_DB_TYPE={{ media_database_type | default('sqlite') }}
MEDIA_DB_HOST={{ media_database_host | default('{{ ansible_default_ipv4.address }}') }}
MEDIA_DB_PORT={{ media_database_port | default(5432) }}
MEDIA_DB_NAME={{ media_database_name | default('media') }}
MEDIA_DB_USER={{ media_database_user | default('media') }}
MEDIA_DB_password: "{{ vault_media_admin_password | password_hash("bcrypt") }}"{{ ansible_default_ipv4.address }}') }}
MEDIA_REDIS_PORT={{ media_redis_port | default(6379) }}
MEDIA_REDIS_password: "{{ vault_media_admin_password | password_hash("bcrypt") }}"0 2 * * *') }}
MEDIA_BACKUP_RETENTION={{ media_backup_retention | default(7) }}
MEDIA_BACKUP_COMPRESSION={{ media_backup_compression | default(true) | lower }}

# Homepage Configuration
MEDIA_HOMEPAGE_ENABLED={{ media_homepage_enabled | default(true) | lower }}
MEDIA_HOMEPAGE_CATEGORY={{ media_homepage_category | default('Media') }}
MEDIA_HOMEPAGE_DESCRIPTION={{ media_homepage_description | default('Media Management & Streaming') }}

# Alerting Configuration
MEDIA_ALERTING_ENABLED={{ media_alerting_enabled | default(true) | lower }}
MEDIA_NOTIFICATIONS_ENABLED={{ media_notifications_enabled | default(true) | lower }}

# Email Configuration
MEDIA_EMAIL_ENABLED={{ media_email_enabled | default(false) | lower }}
MEDIA_SMTP_HOST={{ media_smtp_host | default('localhost') }}
MEDIA_SMTP_PORT={{ media_smtp_port | default(587) }}
MEDIA_SMTP_USERNAME={{ vault_smtp_username | default('') }}
MEDIA_SMTP_password: "{{ vault_media_admin_password | password_hash("bcrypt") }}"tls') }}

# Slack Configuration
MEDIA_SLACK_ENABLED={{ media_slack_enabled | default(false) | lower }}
MEDIA_SLACK_WEBHOOK={{ vault_slack_webhook | default('') }}
MEDIA_SLACK_CHANNEL={{ media_slack_channel | default('#alerts') }}

# Discord Configuration
MEDIA_DISCORD_ENABLED={{ media_discord_enabled | default(false) | lower }}
MEDIA_DISCORD_WEBHOOK={{ vault_discord_webhook | default('') }}
MEDIA_DISCORD_CHANNEL={{ media_discord_channel | default('alerts') }}

# Performance Configuration
MEDIA_PERFORMANCE_MODE={{ media_performance_mode | default('production') }}
MEDIA_CACHE_ENABLED={{ media_cache_enabled | default(true) | lower }}
MEDIA_CACHE_SIZE={{ media_cache_size | default('1g') }}
MEDIA_CACHE_TTL={{ media_cache_ttl | default(3600) }}

# Logging Configuration
MEDIA_LOG_LEVEL={{ media_log_level | default('info') }}
MEDIA_LOG_FORMAT={{ media_log_format | default('json') }}
MEDIA_LOG_RETENTION={{ media_log_retention | default(30) }}

# Resource Limits
MEDIA_MEMORY_LIMIT={{ media_memory_limit | default('8g') }}
MEDIA_CPU_LIMIT={{ media_cpu_limit | default('4.0') }}
MEDIA_STORAGE_LIMIT={{ media_storage_limit | default('500g') }}

# Traefik Configuration
MEDIA_TRAEFIK_ENABLED={{ media_traefik_enabled | default(true) | lower }}
MEDIA_TRAEFIK_SSL_ENABLED={{ media_traefik_ssl_enabled | default(true) | lower }}
MEDIA_TRAEFIK_SSL_RESOLVER={{ media_traefik_ssl_resolver | default('cloudflare') }}

# Maintenance Configuration
MEDIA_MAINTENANCE_ENABLED={{ media_maintenance_enabled | default(true) | lower }}
MEDIA_MAINTENANCE_SCHEDULE={{ media_maintenance_schedule | default('0 3 * * 0') }}
MEDIA_UPDATE_ENABLED={{ media_update_enabled | default(true) | lower }}
MEDIA_UPDATE_SCHEDULE={{ media_update_schedule | default('0 4 * * 0') }}

# Development Configuration
MEDIA_DEBUG_ENABLED={{ media_debug_enabled | default(false) | lower }}
MEDIA_DEBUG_PORT={{ media_debug_port | default(5678) }}
MEDIA_DEBUG_HOST={{ media_debug_host | default('0.0.0.0') }}

# Watchtower Configuration
MEDIA_WATCHTOWER_HTTP_API_TOKEN={{ vault_watchtower_token | default('') }} 