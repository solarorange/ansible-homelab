# Log aggregation config for media stack
logs:
{% for service in media_services %}
  - name: {{ service.name }}
    path: {{ service.log_path }}
    retention_days: {{ service.log_retention | default(30) }}
{% endfor %} 