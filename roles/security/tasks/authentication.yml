---
# This task includes the authentication sub-role.

- name: Deploy security authentication stack
  ansible.builtin.include_role:
    name: security/authentication
  tags: [security, authentication, deploy]
