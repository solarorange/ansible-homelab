---
# Sensitive or environment-specific variables for Security Stack Role
# Override these in group_vars/all/vault.yml (encrypted with ansible-vault)

vault_authentik_admin_password: "{{ vault_security_password }}"
vault_traefik_api_key: ""
vault_pihole_web_password: "{{ vault_security_password }}"
vault_fail2ban_secret_key: ""
vault_crowdsec_api_key: ""
vault_wireguard_private_key: "" 